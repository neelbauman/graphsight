```mermaid
graph TD
    start([Start]) --> define([Define job description])
    define --> request([Request personnel to HR])
    request --> classify([Classify job hiring])
    classify --> reg1{Is it a regular process?}

    reg1 -->|No| special([Special hiring process])
    reg1 -->|Yes| create([Create job posting])

    create --> found{Found a suitable candidate?}
    found -->|No| review([Review previous candidate])
    found -->|Yes| setup([Setup a meeting with candidate])

    review --> suitable{Is candidate suitable for the job?}
    suitable -->|No| plan([Make a hiring plan])
    suitable -->|Yes| setup

    setup --> initial([Conduct initial interview])
    initial --> listq([List interview questions])
    listq --> final1([Conduct final Interview])
    final1 --> ref{Reference checked?}

    plan --> postOnline([Create job posting online])
    postOnline --> collect([Collect submitted resumes])
    collect --> reg2{Is it a regular process?}
    reg2 --> setup

    ref -->|Yes| accept{Candidate accept the offer?}
    ref -->|No| final2([Conduct final Interview])

    accept -->|Yes| hire([Hire the Candidate])
    accept -->|No| final2
```